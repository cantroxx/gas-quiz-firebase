# Claude → Codex 작업 재개 메모 (2026-09-01)

이 문서는 Claude Code의 로컬 메모·세션 기록을 현재 Git·운영 배포본과 교차검증해 만든 지속형 인수인계 문서다. 모델 자체를 영구 학습시키는 문서는 아니지만, 이 저장소에서 작업을 시작할 때 먼저 읽으면 Claude에서 하던 작업의 설계 결정과 중단 지점을 복원할 수 있다.

## 정보 출처와 신뢰 순서

확인한 Claude 자료:

- `/Users/kdw/.claude/projects/-Users-kdw-Projects/memory/`의 프로젝트 메모 13개
- 같은 프로젝트 디렉터리의 Claude 세션 JSONL 20개
- 2026-07-08 이후 Git 커밋 88개
- 현재 작업 트리와 실제 Firebase Hosting 파일
- `/Users/kdw/Projects`의 독립 게임 저장소와 배포 주소

적용 원칙:

1. 현재 코드와 Git을 최우선으로 본다.
2. 배포본 파일과 로컬 파일을 해시로 비교한다.
3. Claude 메모의 “미배포”, “남은 일”은 이후 커밋·배포와 충돌하면 오래된 상태로 처리한다.
4. 원본 세션에는 운영 계정·학생 식별 정보가 섞여 있으므로 원문을 저장소로 복사하지 않는다.

## 현재 안전 기준선

- 브랜치: `main...origin/main`
- 마지막 커밋: `027bffc` — 퀴즈 서바이버 개학 대비 6종 보완
- 현재 미커밋 변경: `public/mathsurvivor/game.js` 한 파일, +214/-44
- `npm run check`: 2026-09-01 통과
- 단, 전체 검사는 중단된 퀴즈 서바이버의 신규 DOM 경로를 실행하지 않아 해당 변경의 안전성을 보증하지 않는다.

운영본과 로컬 비교:

- 메인 `index.html`: 일치
- 하우징 `index.html`, `app.js`: 일치
- 낱말대전 `index.html`, `main.js`: 일치
- 거상 `index.html`: 일치
- 마블 `index.html`: 일치
- 퀴즈 서바이버 `index.html`: 일치
- 퀴즈 서바이버 운영 `game.js`: **현재 미커밋 파일이 아니라 `HEAD`의 정상 파일과 정확히 일치**

따라서 운영 학생 서비스는 현재 로컬의 중단된 퀴즈 서바이버 변경에 영향받지 않는다.

## 최우선 중단 지점: 퀴즈 서바이버

### 마지막 정상 버전

커밋 `027bffc`, 캐시 버전 v7이다.

- 3~6학년, 1·2학기 선택
- 수학 자동생성, 국어·사회·과학·인기·역사 문제은행
- 역사 시간여행 모드와 시대별 그래픽
- 난이도 3단계, 돌진·원거리 몬스터, 러시·보스 패턴
- 무기 5종, 보조장비 8종, 조합 각성, 도감
- 누적 오답노트와 30% 복습 출제
- 효과음 기본 꺼짐
- 점수왕·정답률왕 명예의 전당
- 후반 체력 곡선·엘리트 몬스터

### 사용자가 승인한 다음 묶음

Claude 세션에서 사용자는 다음 A1~A3, B1~B5 전체 진행을 승인했다.

- A1: 어려움에서 한 학년 위 문제 30% + 15초 제한시간·빠른 정답 보너스
- A2: 최종보스 이후 무한 모드
- A3: 주간 챌린지와 주간 랭킹
- B1: 단원 골라 풀기
- B2: 수학 주관식 입력 모드
- B3: 몬스터 없는 공부 모드
- B4: 퀴즈타운의 우리 반 퀴즈 불러오기
- B5: 반 전체 협동 정답 목표

### Claude가 실제로 실행한 작업

Claude는 전체 기능을 구현하지 못했다. 연결 종료 전 Python 치환 스크립트 3개를 실행해 `game.js`만 부분 수정했다.

부분적으로 들어간 코드:

- 어려움 한 학년 위 문제와 15초 타이머 골격
- 단원 필터와 단원 선택 모달 함수
- 수학 주관식 판정 함수
- 공부 모드 시작·종료 함수
- 우리 반 문제, 무한 모드, 주간 챌린지용 상태 변수 선언

완성되지 않은 이유:

- `index.html`에 `unitBody`, `unitModal`, `btnStudyStop`, `quizTimerWrap`, `quizTimerFill`, `quizInputRow`, `quizInput`, `studyResult`가 없다.
- `B.unitList()` 구현이 없다.
- `openUnitModal`, `submitSubjective`, `startStudy`, `stopStudy`를 실행할 UI 바인딩이 없다.
- `classPool`을 Firestore에서 채우는 코드가 없다.
- `endless`, `isChallenge`, 협동 목표는 선언만 있고 동작이 없다.
- CSS, `net.js`, 보안 규칙, 랭킹 스키마, 테스트가 함께 바뀌지 않았다.
- 첫 퀴즈를 열면 존재하지 않는 `btnStudyStop`을 사용하므로 런타임 오류가 발생할 수 있다.

### 안전한 재개 절차

1. 현재 diff를 잃지 않도록 별도 작업 브랜치나 보존 커밋으로 먼저 격리한다. 사용자의 커밋 승인 전에는 실행하지 않는다.
2. 정상 기준선은 `HEAD:public/mathsurvivor/game.js`로 유지한다.
3. A1, B1, B2, B3을 각각 작은 기능 단위로 다시 구현한다. 기존 부분 코드를 그대로 신뢰하지 않는다.
4. A2·A3·B4·B5는 Firestore·랭킹·학생 데이터 영향이 있으므로 별도 설계와 사용자 확인 후 진행한다.
5. 정적 검사는 새 DOM id 존재 여부와 실제 퀴즈 진입을 포함하도록 보강한다.
6. 에뮬레이터에서 문제 출제·시간초과·오답노트·점수 저장을 검증한다.
7. 캐시 버전을 올리고 운영 배포 전에 `npm run check`와 브라우저 회귀를 통과시킨다.

현재 미커밋 `game.js`를 그대로 배포하거나 정상 완성본으로 간주하면 안 된다.

## 퀴즈타운 내부 작업 흐름별 현재 상태

### 하우징·명예의 전당

- 기준 소스: `public/housing/`
- 최신 관련 커밋: `0eb0b14`
- 운영본 `app.js`와 로컬 파일 일치
- 완료: 아이소메트릭 방, 232종 가구 번들, 2,607종 아바타 부품, 방 모양·벽지·바닥, DJ코인 구매, 쿠폰, 기본 선물, 방 저장, 친구집, 방명록·신고·정지, 명예의 전당
- 마지막 피드백 반영: 단차 방을 뒤가 높고 앞이 낮은 무대형으로 재설계, 창문 3종 보류, 벽 아이템 세로 정렬 수정
- 중요 제약:
  - Habbo 에셋은 공개 재배포 금지. Storage의 연결회원 잠금을 유지한다.
  - 번들 변경 시 빌드 스크립트와 `assets-loader.js`의 버전을 함께 올린다.
  - 번들 업로드 후 Hosting을 배포한다.
  - 아바타 이미저는 눕기·걷기 애니메이션에 한계가 있다.
  - `asset-archive`와 `private/housing-assets`를 삭제·공개 커밋하지 않는다.
- 다음 후보: 학생용 단차 방 모델 정식 등록, 창문 벽 마스크 지원, 셀프호스트 아바타 렌더러 검토

### 낱말 대전

- 기준 소스: `public/wordbattle/`
- 운영본 `main.js`와 로컬 파일 일치
- 완료: 공용 보드 재구성, 2~4인 온라인, 봇, 튜토리얼, 80,774단어, 제안·동의, 일시정지·항복·재입장, 랭크
- 타임아웃은 다른 참가자가 대리 처리할 수 있다.
- `wbDraw`는 Functions 콜드스타트가 남은 지연 요인이다. 페이지 진입 예열이 적용되어 있다.
- JS 변경 시 `index.html`의 `?v=N`을 올린다.
- 다음 후보: optimistic UI 또는 함수 분리. 유료 `minInstances`는 비용 승인 전 사용하지 않는다.

### 특산물 마블

- 기준 소스: `marble-src/`
- 빌드 산출물: `public/marble/` — 직접 수정 금지
- 완료: 2~4인, 탈락·항복, 공정한 턴 종료 판정, 좌석 순서 보너스, 재입장, 처리중 잠금
- 순서 보너스 기준: 2인 `[0,225]`, 3인 `[0,220,420]`, 4인 `[0,240,440,660]`
- 다음 변경 전 `marble-src/scripts/simulate-balance.mjs`로 좌석별 승률을 확인한다.

### 팔도 특산물 대상인

- 기준 소스: `geosang-src/`
- 빌드 산출물: `public/geosang/`
- 운영 `index.html`과 로컬 일치
- 완료: 17개 시·도 이동, 특산물 55종, 시장·뉴스·퀘스트·수레·도감·이어하기, 학급 랭킹, 학교·랭킹광장 연결
- 랭킹은 현금이 아니라 누적 도감 수집률 기준이다.
- 특산물 수를 바꾸면 랭킹 광장의 `CODEX_TOTAL=55`도 함께 바꾼다.
- 학급 관리 권한은 학급을 만든 익명 UID 또는 관리자 callable 기준이다.

### 홈타운 소통

- 자유 채팅이 아니라 고정 문구 8개와 스탬프만 사용한다.
- presence 하트비트는 1분, 만료는 2.5분이다.
- 교사 계정은 admin custom claim을 보장하지 않으므로 관리자 기능은 callable을 사용한다.
- 다른 기기에서 재로그인하면 authUid가 바뀌어 이전 기기의 안 읽은 쪽지가 이어지지 않을 수 있다.

### 문화유산 게임

- 운영 원본: `/Users/kdw/Projects/seoul-heritage-main`
- 저장소 이름은 `cantroxx/seoul-heritage`다.
- 삭제된 테스트 폴더·저장소를 다시 기준으로 삼지 않는다.
- 랭킹전은 임시 사수 세트, 학습은 영구 사수 세트로 분리되어 있다.
- Firebase 학생 데이터와 랭킹 구조를 변경하기 전 사용자 확인이 필요하다.

## 독립 게임에서 이어받은 핵심 결정

세부 현황은 `docs/product/GAME_PROJECT_INVENTORY_2026-09-01.md`에 있다.

- 배틀 스쿨: 퀴즈타운과 독립 유지. 다음 핵심은 직업·스킬·약점으로 선택의 깊이를 만드는 것
- KBO 구단주: 퀴즈타운과 독립 유지. 엔진 수치와 훈련 구조는 안정적이며 데이터 검수와 FM식 깊이가 다음 과제
- 인생 그림일기: 독립 유지. Git 기준점과 이미지 투입이 다음 과제
- 특산물 마블 독립판: 퀴즈타운 통합판의 기준 소스가 아님
- 하우징 체험판·방 프로토타입: 현재 통합 하우징의 선조이며 에셋 보관·실험 용도

## 작업 재개 체크리스트

1. `git status --short --branch`로 이 문서 이후 변경을 확인한다.
2. 요청 영역의 Claude 메모보다 현재 코드·Git을 우선한다.
3. 운영본과 로컬이 다르면 배포부터 하지 말고 차이의 출처를 설명한다.
4. 학생 데이터·보안 규칙·Functions·Storage 변경은 영향과 검증법을 먼저 설명하고 승인을 받는다.
5. `public/index.html` 공유 셸은 병렬 편집하지 않는다.
6. React 하위 앱은 소스 폴더를 수정하고 빌드 산출물을 직접 고치지 않는다.
7. 기능별 정적 검사, 에뮬레이터, 브라우저 실플레이 순서로 검증한다.
8. 요청 없는 commit, push, deploy는 하지 않는다.
